{% extends 'MinicoSilverBundle::base.html.twig' %}

{% block body -%}
    <h1>Withdrawls list</h1>

    <ul>
        <li>
            <a href="{{ path('withdrawls_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>Storage from</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('withdrawls_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.quantity }}</td>
                <td>{{ entity.storage }}</td>
                <td>{% if entity.date %}{{ entity.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('withdrawls_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('withdrawls_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('withdrawls_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
